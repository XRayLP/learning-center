{% extends '@LearningCenter/base.html.twig' %}

{% block main %}
    <div class="dashboard">
        <div class="card">
            <div class="card-body">
                <h5>{% trans %}Welcome %name%, in the Learning Center of the School %schoolname%.{% endtrans %}</h5>
            </div>
        </div>
    </div>

    <div class="dashboard">
        <div class="card">
            <div class="card-header">
                Storage Usage
            </div>
            <div class="card-body">
                <h3>{{ storage.0 }}%</h3>
                <p>{{ storage.1 }} von {{ storage.2 }} belegt.</p>
            </div>
        </div>
    </div>
{% endblock %}